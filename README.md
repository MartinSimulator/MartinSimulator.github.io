# simple-website

Blank React + TypeScript app built with Vite.

## Scripts

- `npm run dev` – start the development server
- `npm run build` – typecheck and build for production
- `npm run preview` – preview the production build
- `npm run lint` – run oxlint
